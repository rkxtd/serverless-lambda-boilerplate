machine:
  timezone:
    Europe/Kiev
  node:
    version:
      6.10.0
  environment:
    CIRCLE_ENV: test
dependencies:
  pre:
  override:
    - npm install
  cache_directories:
    - "node_modules"
general:
  artifacts:
    - "coverage/coverage.json"
test:
  override:
    # - npm run test-report
  post:
    #- if [ $CIRCLE_BRANCH = 'master' ]; then npm run redeploy; fi
    - npm run redeploy
notify:
  webhooks:
    - url: https://hooks.slack.com/services/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